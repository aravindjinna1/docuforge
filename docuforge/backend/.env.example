# ─── Server ────────────────────────────────────────
PORT=4000
NODE_ENV=development

# ─── Database ──────────────────────────────────────
# Local MongoDB
MONGODB_URI=

# MongoDB Atlas (production)  aravindjinna1_db_user
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/docucraft?retryWrites=true&w=majority

# ─── AI Providers (set at least one) ──────────────
# Google Gemini — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
# Groq (fallback) — https://console.groq.com/keys
GROQ_API_KEY=
# ─── Frontend ──────────────────────────────────────
FRONTEND_URL=http://localhost:3000
